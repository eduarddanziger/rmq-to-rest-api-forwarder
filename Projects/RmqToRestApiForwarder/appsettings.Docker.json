{
    "RabbitMQ": {
        "Service": {
            "HostName": "rabbitmq",
            "UserName": "forwarder",
            "Password": "forwarder"
        }
    },
    "NLog": {
        "variables": {
            "logdir": "/app/logs",
            "logname": "RmqToRestApiForwarder"
        },
        "targets": {
            "own-console": {
                "target": {
                    "layout": "${date:format=yyyy-MM-dd HH\\:mm\\:ss.ffffff} ${level:uppercase=false:format=FirstCharacter} ${message}"
                }
            }
        }
    }
}
